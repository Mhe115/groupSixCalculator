import java.util.Scanner;
public class Calculator {

    public static String calculation = "";
    public static void main(String[] args) {
        Scanner input = new Scanner(System.in);
        System.out.println("Enter n for numerical calculations or q for quadratic calculations");
        String nextIn = input.nextLine();
        if (nextIn.contains("n")) {
            System.out.println("Use p for Pi and e for Euler's number");
            System.out.println("Enter your calculation: ");
            calculation = input.nextLine();
            SyntaxHandler SyntaxHandler = new SyntaxHandler();
            if (SyntaxHandler.verifiyString(calculation)) {
                Operations.calc(calculation); 
            }

        }
        if (nextIn.contains("q")) {
            //quadratic stuff
        }

        input.close();

    }
}